# Commits that only moved code around, so that blame can look past them to
# whoever actually wrote the line. GitHub reads this file on its own; locally:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# Run Prettier over everything, once
eee4747a8ad8a117218dd0330c94c2d78722ed2d
